const express = require("express");
const router = express.Router();
const {
 
  
 
} = require("../../../../controllers/v1/admin/appointments/appointments.controller.js");

router.post("/t/:user_id", (req, res) => {
});


module.exports = router;