const express = require("express");
const router = express.Router();
const {
    
} = require("../../../../controllers/v1/admin/doctor/doctor.controller.js");

router.post("/t/:user_id", (req, res) => {});

module.exports = router;
